#!/usr/bin/env bash
set -euo pipefail

usage() {
  cat <<'EOF'
Envisaged (Nix-first)

Usage:
  envisaged [options] <repo-path-or-git-url>
  envisaged [options] --multi-dir <directory-containing-git-repos>

Options:
  -o, --output <path>           Output file path (default: output.mp4)
  -r, --resolution <preset>     2160p | 1440p | 1080p | 720p (default: 1080p)
  -f, --fps <fps>               25 | 30 | 60 (default: 60)
  -t, --title <text>            Video title
      --template <name>         none | border | neon | sunset | matrix | blueprint | noir |
                                 compare-panel | compare-neon | compare-blueprint | compare-matrix | compare-noir |
                                 split-quad | split-vertical | split-triple | split-focus | split-matrix |
                                 relation-panel | relation-neon | relation-blueprint | relation-noir | relation-sunset
      --logo <path-or-url>      Optional logo image (local path or URL)
      --multi-dir <path>        Merge multiple repos from a directory

Compare mode options:
      --sync-timing <mode>      auto | true | false | smart (default: auto)
      --sync-span <seconds>     Normalized timeline span for compare mode (default: 31536000)

Advanced:
      --seconds-per-day <num>   (default: 0.12)
      --time-scale <num>        0.0 - 4.0 (default: 1.6)
      --user-scale <num>        (default: 1.35)
      --auto-skip <num>         (default: 0.5)
      --crf <num>               x264 CRF (default: 22)
      --preset <name>           x264 preset (default: medium)

Examples:
  envisaged .
  envisaged -o demo.mp4 -r 720p -f 30 -t "Envisaged Evolution" .
  envisaged --logo ./logo.png https://github.com/utensils/Envisaged.git
  envisaged --multi-dir ~/Projects/utensils --template compare-panel -t "Nix Gen Repos"
EOF
}

OUTPUT="output.mp4"
VIDEO_RESOLUTION="1080p"
GOURCE_FPS="60"
GOURCE_TITLE="Software Development"
TEMPLATE="border"
LOGO_INPUT=""
MULTI_DIR=""
INPUT_REPO=""
SYNC_TIMING="auto"
SYNC_SPAN="31536000"

GOURCE_SECONDS_PER_DAY="0.12"
GOURCE_TIME_SCALE="1.6"
GOURCE_USER_SCALE="1.35"
GOURCE_AUTO_SKIP="0.5"
H264_CRF="22"
H264_PRESET="medium"

while [[ $# -gt 0 ]]; do
  case "$1" in
    -h|--help) usage; exit 0 ;;
    -o|--output) OUTPUT="$2"; shift 2 ;;
    -r|--resolution) VIDEO_RESOLUTION="$2"; shift 2 ;;
    -f|--fps) GOURCE_FPS="$2"; shift 2 ;;
    -t|--title) GOURCE_TITLE="$2"; shift 2 ;;
    --template) TEMPLATE="$2"; shift 2 ;;
    --logo) LOGO_INPUT="$2"; shift 2 ;;
    --multi-dir) MULTI_DIR="$2"; shift 2 ;;
    --sync-timing) SYNC_TIMING="$2"; shift 2 ;;
    --sync-span) SYNC_SPAN="$2"; shift 2 ;;
    --seconds-per-day) GOURCE_SECONDS_PER_DAY="$2"; shift 2 ;;
    --time-scale) GOURCE_TIME_SCALE="$2"; shift 2 ;;
    --user-scale) GOURCE_USER_SCALE="$2"; shift 2 ;;
    --auto-skip) GOURCE_AUTO_SKIP="$2"; shift 2 ;;
    --crf) H264_CRF="$2"; shift 2 ;;
    --preset) H264_PRESET="$2"; shift 2 ;;
    --) shift; break ;;
    -*) echo "Unknown option: $1" >&2; usage >&2; exit 1 ;;
    *)
      if [[ -z "${INPUT_REPO}" ]]; then
        INPUT_REPO="$1"
      else
        echo "Unexpected extra argument: $1" >&2
        usage >&2
        exit 1
      fi
      shift
      ;;
  esac
done

if [[ $# -gt 0 && -z "${INPUT_REPO}" ]]; then
  INPUT_REPO="$1"
  shift
fi

if [[ $# -gt 0 ]]; then
  echo "Unexpected extra args: $*" >&2
  exit 1
fi

if [[ -z "${MULTI_DIR}" && -z "${INPUT_REPO}" ]]; then
  echo "Provide either a repo input or --multi-dir." >&2
  usage >&2
  exit 1
fi

if [[ -n "${MULTI_DIR}" && -n "${INPUT_REPO}" ]]; then
  echo "Use either <repo> OR --multi-dir, not both." >&2
  exit 1
fi

case "${VIDEO_RESOLUTION}" in
  2160p) WIDTH=3840; HEIGHT=2160 ;;
  1440p) WIDTH=2560; HEIGHT=1440 ;;
  1080p) WIDTH=1920; HEIGHT=1080 ;;
  720p)  WIDTH=1280; HEIGHT=720 ;;
  *) echo "Unsupported resolution: ${VIDEO_RESOLUTION}" >&2; exit 1 ;;
esac

case "${GOURCE_FPS}" in
  25|30|60) ;;
  *) echo "Unsupported fps: ${GOURCE_FPS} (supported: 25,30,60)" >&2; exit 1 ;;
esac

case "${TEMPLATE}" in
  none|border|neon|sunset|matrix|blueprint|noir|compare-panel|compare-neon|compare-blueprint|compare-matrix|compare-noir|split-quad|split-vertical|split-triple|split-focus|split-matrix|relation-panel|relation-neon|relation-blueprint|relation-noir|relation-sunset) ;;
  *) echo "Unsupported template: ${TEMPLATE}" >&2; exit 1 ;;
esac

case "${SYNC_TIMING}" in
  auto|true|false|smart) ;;
  *) echo "Unsupported --sync-timing value: ${SYNC_TIMING}" >&2; exit 1 ;;
esac

if ! [[ "${SYNC_SPAN}" =~ ^[0-9]+$ ]]; then
  echo "--sync-span must be an integer number of seconds" >&2
  exit 1
fi

is_compare_template=false
is_split_template=false
is_relation_template=false
case "${TEMPLATE}" in
  compare-*) is_compare_template=true ;;
esac
case "${TEMPLATE}" in
  split-*) is_split_template=true ;;
esac
case "${TEMPLATE}" in
  relation-*) is_relation_template=true ;;
esac

if [[ "${SYNC_TIMING}" == "auto" ]]; then
  if [[ -n "${MULTI_DIR}" && ( "${is_compare_template}" == true || "${is_split_template}" == true || "${is_relation_template}" == true ) ]]; then
    SYNC_TIMING="smart"
  else
    SYNC_TIMING="false"
  fi
fi

workdir="$(mktemp -d)"
cleanup() { rm -rf "${workdir}"; }
trap cleanup EXIT

log_dir="${workdir}/logs"
mkdir -p "${log_dir}"

declare -a REPO_NAMES=()
declare -a REPO_LOGS=()

clone_or_use_repo() {
  local src="$1"
  local out_var="$2"
  local repo_dir="$src"

  if [[ "${src}" =~ ^https?:// || "${src}" =~ ^git@ ]]; then
    repo_dir="${workdir}/repo"
    echo "Cloning ${src} ..."
    git clone --quiet "${src}" "${repo_dir}"
  fi

  if [[ ! -d "${repo_dir}/.git" ]]; then
    echo "Not a git repository: ${repo_dir}" >&2
    exit 1
  fi

  printf -v "$out_var" '%s' "$repo_dir"
}

normalize_log_timestamps() {
  local in_log="$1"
  local out_log="$2"
  local sync_span="$3"
  local base_epoch=946684800

  local min_ts max_ts
  min_ts=$(awk -F'|' 'NR==1{m=$1} $1<m{m=$1} END{print m+0}' "$in_log")
  max_ts=$(awk -F'|' 'NR==1{m=$1} $1>m{m=$1} END{print m+0}' "$in_log")

  awk -F'|' -v OFS='|' -v min="$min_ts" -v max="$max_ts" -v target="$sync_span" -v base="$base_epoch" '
    {
      span = max - min;
      if (span <= 0) span = 1;
      t = int(((($1 - min) / span) * target) + base);
      print t, $2, $3, $4;
    }
  ' "$in_log" > "$out_log"
}

inject_sync_blanks() {
  local in_log="$1"
  local out_log="$2"
  local sync_span="$3"
  local repo_name="$4"

  local start_ts end_ts
  start_ts=$(awk -F'|' 'NR==1{m=$1} $1<m{m=$1} END{print m+0}' "$in_log")
  end_ts=$(awk -F'|' 'NR==1{m=$1} $1>m{m=$1} END{print m+0}' "$in_log")

  local interval=$((sync_span / 8))
  if [[ $interval -lt 1 ]]; then
    interval=1
  fi

  cp "$in_log" "${out_log}.tmp"
  echo "${start_ts}|_sync_|M|/${repo_name}/.sync/anchor" >> "${out_log}.tmp"
  echo "${end_ts}|_sync_|M|/${repo_name}/.sync/anchor" >> "${out_log}.tmp"

  local t=$((start_ts + interval))
  while [[ $t -lt $end_ts ]]; do
    echo "${t}|_sync_|M|/${repo_name}/.sync/pulse" >> "${out_log}.tmp"
    t=$((t + interval))
  done

  sort -n "${out_log}.tmp" > "$out_log"
  rm -f "${out_log}.tmp"
}

build_single_log() {
  local repo_dir="$1"
  local out_log="$2"
  gource --output-custom-log "$out_log" "$repo_dir" >/dev/null
}

start_gource_to_pipe() {
  local log_file="$1"
  local pipe_file="$2"
  local render_title="$3"

  SDL_VIDEODRIVER=x11 \
  xvfb-run -a -s "-screen 0 ${WIDTH}x${HEIGHT}x24" \
    gource \
      --seconds-per-day "${GOURCE_SECONDS_PER_DAY}" \
      --user-scale "${GOURCE_USER_SCALE}" \
      --time-scale "${GOURCE_TIME_SCALE}" \
      --auto-skip-seconds "${GOURCE_AUTO_SKIP}" \
      --title "${render_title}" \
      --background-colour 000000 \
      --font-colour FFFFFF \
      --camera-mode overview \
      --hide usernames,mouse,date,filenames \
      --font-size 42 \
      --dir-name-depth 3 \
      --filename-time 2 \
      --max-user-speed 500 \
      --bloom-multiplier 1.2 \
      --${INNER_W}x${INNER_H} \
      --stop-at-end \
      "$log_file" \
      -r "${GOURCE_FPS}" \
      -o - > "$pipe_file" &

  echo $!
}

build_multi_log() {
  local base_dir="$1"
  local out_log="$2"
  local tmp_list=()

  shopt -s nullglob
  for d in "$base_dir"/*; do
    if [[ -d "$d/.git" ]]; then
      local name
      name="$(basename "$d")"
      REPO_NAMES+=("$name")

      local raw_log="${log_dir}/${name}.raw.log"
      local prepared_log="${log_dir}/${name}.log"

      echo "Collecting: $name"
      gource --output-custom-log "$raw_log" "$d" >/dev/null

      if [[ "${SYNC_TIMING}" == "true" || "${SYNC_TIMING}" == "smart" ]]; then
        normalize_log_timestamps "$raw_log" "$prepared_log" "$SYNC_SPAN"
        if [[ "${SYNC_TIMING}" == "smart" ]]; then
          inject_sync_blanks "$prepared_log" "${prepared_log}.smart" "$SYNC_SPAN" "$name"
          mv "${prepared_log}.smart" "$prepared_log"
        fi
      else
        cp "$raw_log" "$prepared_log"
      fi

      sed -i -E "s#^([0-9]+\|[^|]+\|[^|]+\|)#\1/${name}#" "$prepared_log"
      REPO_LOGS+=("$prepared_log")
      tmp_list+=("$prepared_log")
    fi
  done
  shopt -u nullglob

  if [[ ${#tmp_list[@]} -eq 0 ]]; then
    echo "No git repos found in: $base_dir" >&2
    exit 1
  fi

  cat "${tmp_list[@]}" | sort -n > "$out_log"
}

build_legend_file() {
  local f="$1"
  {
    echo "COMPARE LEGEND"
    echo
    for repo in "${REPO_NAMES[@]}"; do
      echo "• $repo"
    done
    if [[ "${SYNC_TIMING}" == "true" || "${SYNC_TIMING}" == "smart" ]]; then
      echo
      echo "timing: unified"
      if [[ "${SYNC_TIMING}" == "smart" ]]; then
        echo "sync: smart blank-log pulses"
      fi
    fi
  } > "$f"
}

build_relationship_file() {
  local f="$1"
  local max_rel=8
  local count=0

  {
    echo "RELATIONSHIPS"
    echo

    for ((i=0; i<${#REPO_NAMES[@]}; i++)); do
      for ((j=i+1; j<${#REPO_NAMES[@]}; j++)); do
        local a="${REPO_NAMES[$i]}"
        local b="${REPO_NAMES[$j]}"
        local abase="${a%-nix}"
        local bbase="${b%-nix}"

        if [[ "$abase" == *"$bbase"* || "$bbase" == *"$abase"* ]]; then
          echo "• $a ↔ $b (variant line)"
          count=$((count + 1))
        else
          local shared=""
          IFS='-' read -r -a toks <<< "$abase"
          for tok in "${toks[@]}"; do
            if [[ ${#tok} -ge 4 && "$bbase" == *"$tok"* ]]; then
              shared="$tok"
              break
            fi
          done
          if [[ -n "$shared" ]]; then
            echo "• $a ↔ $b (shared: $shared)"
            count=$((count + 1))
          fi
        fi

        if [[ $count -ge $max_rel ]]; then
          break 2
        fi
      done
    done

    if [[ $count -eq 0 ]]; then
      echo "• Similar stack family inferred from naming and cadence"
    fi
  } > "$f"
}

DEVLOG="${workdir}/development.log"
if [[ -n "${MULTI_DIR}" ]]; then
  build_multi_log "${MULTI_DIR}" "$DEVLOG"
else
  REPO_DIR=""
  clone_or_use_repo "${INPUT_REPO}" REPO_DIR
  build_single_log "$REPO_DIR" "$DEVLOG"
fi

LOGO_FILE=""
if [[ -n "${LOGO_INPUT}" ]]; then
  if [[ "${LOGO_INPUT}" =~ ^https?:// ]]; then
    LOGO_FILE="${workdir}/logo"
    echo "Downloading logo: ${LOGO_INPUT}"
    curl -fsSL "${LOGO_INPUT}" -o "$LOGO_FILE"
  else
    if [[ ! -f "${LOGO_INPUT}" ]]; then
      echo "Logo not found: ${LOGO_INPUT}" >&2
      exit 1
    fi
    LOGO_FILE="$LOGO_INPUT"
  fi
fi

if [[ "${TEMPLATE}" == "border" || "${TEMPLATE}" == "neon" || "${TEMPLATE}" == "blueprint" || "${TEMPLATE}" == compare-* || "${TEMPLATE}" == split-* || "${TEMPLATE}" == relation-* ]]; then
  FRAME=26
else
  FRAME=0
fi
INNER_W=$((WIDTH - (FRAME * 2)))
INNER_H=$((HEIGHT - (FRAME * 2)))

declare -a PIPES=()
declare -a GOURCE_PIDS=()
declare -a QUAD_REPO_NAMES=()
USE_QUAD_MULTI_REPOS=false

if [[ "${TEMPLATE}" == "split-quad" && -n "${MULTI_DIR}" && ${#REPO_LOGS[@]} -ge 4 ]]; then
  USE_QUAD_MULTI_REPOS=true
  QUAD_REPO_NAMES=("${REPO_NAMES[@]:0:4}")
  REPO_NAMES=("${QUAD_REPO_NAMES[@]}")
else
  PIPE="${workdir}/gource.pipe"
  mkfifo "$PIPE"
  PIPES+=("$PIPE")
  pid_main=$(start_gource_to_pipe "$DEVLOG" "$PIPE" "${GOURCE_TITLE}")
  GOURCE_PIDS+=("$pid_main")
fi

echo "Rendering: ${OUTPUT}"
echo "Resolution: ${WIDTH}x${HEIGHT} (${VIDEO_RESOLUTION}), fps=${GOURCE_FPS}, template=${TEMPLATE}, sync=${SYNC_TIMING}"
if [[ "${USE_QUAD_MULTI_REPOS}" == true ]]; then
  echo "Quad mode: using 4 distinct repos (${QUAD_REPO_NAMES[*]})"
  BASE_INPUT_COUNT=4
else
  BASE_INPUT_COUNT=${#PIPES[@]}
fi

USE_COMPLEX=false
FILTER=""
COMPLEX_FILTER=""
FINAL_LABEL=""

case "${TEMPLATE}" in
  none)
    FILTER=""
    ;;
  border)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#222226,drawbox=x=0:y=0:w=iw:h=ih:color=#4d4d54@0.85:t=5"
    ;;
  neon)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#05030d,eq=saturation=1.45:contrast=1.20:brightness=-0.02,curves=all='0/0 0.35/0.22 0.7/0.88 1/1',drawgrid=w=80:h=80:t=1:c=#00ffd0@0.12,drawbox=x=0:y=0:w=iw:h=ih:color=#00ffd0@0.55:t=6"
    ;;
  sunset)
    FILTER="colorbalance=rs=.12:gs=-.02:bs=-.10,eq=saturation=1.30:contrast=1.08:brightness=0.01,vignette=PI/7"
    ;;
  matrix)
    FILTER="hue=s=0,curves=all='0/0 1/0.95',colorchannelmixer=rr=0:rg=0:rb=0:gr=0.15:gg=1.0:gb=0.05:br=0:bg=0:bb=0,noise=alls=8:allf=t,drawgrid=w=64:h=28:t=1:c=#00ff66@0.09"
    ;;
  blueprint)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#0b1e3f,colorchannelmixer=rr=0.10:rg=0.12:rb=0.30:gr=0.12:gg=0.50:gb=0.75:br=0.35:bg=0.55:bb=1.00,eq=saturation=0.75:contrast=1.12,drawgrid=w=90:h=90:t=1:c=#9fd0ff@0.16,drawbox=x=0:y=0:w=iw:h=ih:color=#9fd0ff@0.45:t=5"
    ;;
  noir)
    FILTER="hue=s=0,eq=contrast=1.25:brightness=-0.04,gblur=sigma=0.45,vignette=PI/5,drawbox=x=0:y=0:w=iw:h=ih:color=#ffffff@0.12:t=2"
    ;;
  compare-panel)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#1a1a21,eq=saturation=1.05:contrast=1.06,drawbox=x=0:y=0:w=iw:h=ih:color=#90b4ff@0.35:t=4"
    ;;
  compare-neon)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#03060c,eq=saturation=1.4:contrast=1.2,drawgrid=w=70:h=70:t=1:c=#00ffd0@0.1,drawbox=x=0:y=0:w=iw:h=ih:color=#00ffd0@0.45:t=5"
    ;;
  compare-blueprint)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#0b1e3f,colorchannelmixer=rr=0.10:rg=0.12:rb=0.30:gr=0.12:gg=0.50:gb=0.75:br=0.35:bg=0.55:bb=1.00,eq=saturation=0.8:contrast=1.12,drawgrid=w=84:h=84:t=1:c=#9fd0ff@0.16,drawbox=x=0:y=0:w=iw:h=ih:color=#9fd0ff@0.45:t=5"
    ;;
  compare-matrix)
    FILTER="hue=s=0,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.17:gg=1.0:gb=0.05:br=0:bg=0:bb=0,noise=alls=6:allf=t,drawgrid=w=64:h=28:t=1:c=#00ff66@0.09,drawbox=x=0:y=0:w=iw:h=ih:color=#00ff66@0.28:t=4"
    ;;
  compare-noir)
    FILTER="hue=s=0,eq=contrast=1.25:brightness=-0.04,gblur=sigma=0.45,vignette=PI/5,drawbox=x=0:y=0:w=iw:h=ih:color=#ffffff@0.22:t=3"
    ;;
  relation-panel)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#1a1a21,eq=saturation=1.08:contrast=1.08,drawbox=x=0:y=0:w=iw:h=ih:color=#90b4ff@0.35:t=4"
    ;;
  relation-neon)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#03060c,eq=saturation=1.4:contrast=1.2,drawgrid=w=70:h=70:t=1:c=#00ffd0@0.1,drawbox=x=0:y=0:w=iw:h=ih:color=#00ffd0@0.45:t=5"
    ;;
  relation-blueprint)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#0b1e3f,colorchannelmixer=rr=0.10:rg=0.12:rb=0.30:gr=0.12:gg=0.50:gb=0.75:br=0.35:bg=0.55:bb=1.00,eq=saturation=0.8:contrast=1.12,drawgrid=w=84:h=84:t=1:c=#9fd0ff@0.16,drawbox=x=0:y=0:w=iw:h=ih:color=#9fd0ff@0.45:t=5"
    ;;
  relation-noir)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#151515,hue=s=0,eq=contrast=1.22:brightness=-0.03,vignette=PI/6,drawbox=x=0:y=0:w=iw:h=ih:color=#e6e6e6@0.28:t=4"
    ;;
  relation-sunset)
    FILTER="pad=${WIDTH}:${HEIGHT}:${FRAME}:${FRAME}:#24121a,colorbalance=rs=.15:gs=-.03:bs=-.10,eq=saturation=1.26:contrast=1.08,vignette=PI/7,drawbox=x=0:y=0:w=iw:h=ih:color=#ffb06a@0.35:t=4"
    ;;
  split-quad)
    USE_COMPLEX=true
    if [[ "${USE_QUAD_MULTI_REPOS}" == true ]]; then
      COMPLEX_FILTER="[0:v]scale=iw/2:ih/2,eq=saturation=1.25:contrast=1.1[a1];[1:v]scale=iw/2:ih/2,hue=s=0,eq=contrast=1.2[b1];[2:v]scale=iw/2:ih/2,colorbalance=rs=.12:gs=-.02:bs=-.08[c1];[3:v]scale=iw/2:ih/2,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.2:gg=1.0:gb=0.05:br=0:bg=0:bb=0[d1];[a1][b1][c1][d1]xstack=inputs=4:layout=0_0|w0_0|0_h0|w0_h0[outv]"
    else
      COMPLEX_FILTER="[0:v]split=4[a][b][c][d];[a]scale=iw/2:ih/2,eq=saturation=1.25:contrast=1.1[a1];[b]scale=iw/2:ih/2,hue=s=0,eq=contrast=1.2[b1];[c]scale=iw/2:ih/2,colorbalance=rs=.12:gs=-.02:bs=-.08[c1];[d]scale=iw/2:ih/2,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.2:gg=1.0:gb=0.05:br=0:bg=0:bb=0[d1];[a1][b1][c1][d1]xstack=inputs=4:layout=0_0|w0_0|0_h0|w0_h0[outv]"
    fi
    FINAL_LABEL="outv"
    ;;
  split-vertical)
    USE_COMPLEX=true
    COMPLEX_FILTER="[0:v]split=2[a][b];[a]eq=saturation=1.3:contrast=1.12[a1];[b]hue=s=0,eq=contrast=1.25[b1];[a1][b1]hstack=inputs=2[outv]"
    FINAL_LABEL="outv"
    ;;
  split-triple)
    USE_COMPLEX=true
    COMPLEX_FILTER="[0:v]split=3[a][b][c];[a]crop=iw/2:ih:0:0,eq=saturation=1.22:contrast=1.08[a1];[b]crop=iw/4:ih:iw/4:0,hue=s=0,eq=contrast=1.2[b1];[c]crop=iw/4:ih:3*iw/4:0,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.18:gg=1.0:gb=0.05:br=0:bg=0:bb=0[c1];[a1][b1][c1]hstack=inputs=3[outv]"
    FINAL_LABEL="outv"
    ;;
  split-focus)
    USE_COMPLEX=true
    COMPLEX_FILTER="[0:v]split=2[a][b];[a]scale=iw:ih,eq=saturation=1.18:contrast=1.1,drawbox=x=8:y=8:w=iw-16:h=ih-16:color=#87b7ff@0.35:t=3[base];[b]scale=iw/3:ih/3,hue=s=0,eq=contrast=1.24[mini];[base][mini]overlay=W-w-28:28[outv]"
    FINAL_LABEL="outv"
    ;;
  split-matrix)
    USE_COMPLEX=true
    COMPLEX_FILTER="[0:v]split=4[a][b][c][d];[a]scale=iw/2:ih/2,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.2:gg=1.0:gb=0.05:br=0:bg=0:bb=0[a1];[b]scale=iw/2:ih/2,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.15:gg=0.9:gb=0.03:br=0:bg=0:bb=0[b1];[c]scale=iw/2:ih/2,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.22:gg=1.05:gb=0.07:br=0:bg=0:bb=0[c1];[d]scale=iw/2:ih/2,colorchannelmixer=rr=0:rg=0:rb=0:gr=0.18:gg=0.95:gb=0.05:br=0:bg=0:bb=0[d1];[a1][b1][c1][d1]xstack=inputs=4:layout=0_0|w0_0|0_h0|w0_h0,drawgrid=w=64:h=28:t=1:c=#00ff66@0.12[outv]"
    FINAL_LABEL="outv"
    ;;
esac

legend_needed=false
if [[ ${#REPO_NAMES[@]} -gt 0 && ( "${is_compare_template}" == true || "${is_split_template}" == true || "${is_relation_template}" == true ) ]]; then
  legend_needed=true
fi

if [[ "${legend_needed}" == true ]]; then
  LEGEND_FILE="${workdir}/legend.txt"
  build_legend_file "$LEGEND_FILE"
  legend_h=$((150 + (${#REPO_NAMES[@]} * 34)))
  legend_filter="drawbox=x=18:y=18:w=460:h=${legend_h}:color=#000000@0.38:t=fill,drawbox=x=18:y=18:w=460:h=${legend_h}:color=#d8e7ff@0.22:t=2,drawtext=textfile=${LEGEND_FILE}:x=34:y=36:fontsize=22:fontcolor=white:line_spacing=10"

  if [[ "${USE_COMPLEX}" == true ]]; then
    COMPLEX_FILTER="${COMPLEX_FILTER};[${FINAL_LABEL}]${legend_filter}[outleg]"
    FINAL_LABEL="outleg"
  else
    if [[ -n "${FILTER}" ]]; then
      FILTER="${FILTER},${legend_filter}"
    else
      FILTER="${legend_filter}"
    fi
  fi
fi

if [[ "${is_relation_template}" == true && ${#REPO_NAMES[@]} -gt 1 ]]; then
  REL_FILE="${workdir}/relationships.txt"
  build_relationship_file "$REL_FILE"
  rel_h=$((170 + (${#REPO_NAMES[@]} * 24)))
  rel_filter="drawbox=x=iw-560:y=18:w=542:h=${rel_h}:color=#000000@0.38:t=fill,drawbox=x=iw-560:y=18:w=542:h=${rel_h}:color=#ffd28a@0.24:t=2,drawtext=textfile=${REL_FILE}:x=w-540:y=36:fontsize=20:fontcolor=white:line_spacing=8"

  if [[ "${USE_COMPLEX}" == true ]]; then
    COMPLEX_FILTER="${COMPLEX_FILTER};[${FINAL_LABEL}]${rel_filter}[outrel]"
    FINAL_LABEL="outrel"
  else
    if [[ -n "${FILTER}" ]]; then
      FILTER="${FILTER},${rel_filter}"
    else
      FILTER="${rel_filter}"
    fi
  fi
fi

if [[ -n "${LOGO_FILE}" ]]; then
  logo_input_index="${BASE_INPUT_COUNT}"
  if [[ "${USE_COMPLEX}" == true ]]; then
    COMPLEX_FILTER="${COMPLEX_FILTER};[${logo_input_index}:v]scale=-1:${HEIGHT}/8[logo];[${FINAL_LABEL}][logo]overlay=W-w-40:H-h-40[outlogo]"
    FINAL_LABEL="outlogo"
  else
    if [[ -n "${FILTER}" ]]; then
      FILTER="${FILTER}[base];[${logo_input_index}:v]scale=-1:${HEIGHT}/8[logo];[base][logo]overlay=W-w-40:H-h-40"
      USE_COMPLEX=true
      COMPLEX_FILTER="[0:v]${FILTER}[outlogo]"
      FINAL_LABEL="outlogo"
    else
      USE_COMPLEX=true
      COMPLEX_FILTER="[${logo_input_index}:v]scale=-1:${HEIGHT}/8[logo];[0:v][logo]overlay=W-w-40:H-h-40[outlogo]"
      FINAL_LABEL="outlogo"
    fi
  fi
fi

if [[ "${USE_QUAD_MULTI_REPOS}" == true ]]; then
  declare -a QUAD_TMP_VIDEOS=()

  for i in 0 1 2 3; do
    qv="${workdir}/quad-src-${i}.mp4"
    QUAD_TMP_VIDEOS+=("$qv")

    SDL_VIDEODRIVER=x11 \
    xvfb-run -a -s "-screen 0 ${WIDTH}x${HEIGHT}x24" \
      gource \
        --seconds-per-day "${GOURCE_SECONDS_PER_DAY}" \
        --user-scale "${GOURCE_USER_SCALE}" \
        --time-scale "${GOURCE_TIME_SCALE}" \
        --auto-skip-seconds "${GOURCE_AUTO_SKIP}" \
        --title "${GOURCE_TITLE} — ${QUAD_REPO_NAMES[$i]}" \
        --background-colour 000000 \
        --font-colour FFFFFF \
        --camera-mode overview \
        --hide usernames,mouse,date,filenames \
        --font-size 42 \
        --dir-name-depth 3 \
        --filename-time 2 \
        --max-user-speed 500 \
        --bloom-multiplier 1.2 \
        --${INNER_W}x${INNER_H} \
        --stop-at-end \
        "${REPO_LOGS[$i]}" \
        -r "${GOURCE_FPS}" \
        -o - | \
      ffmpeg -y \
        -r "${GOURCE_FPS}" -f image2pipe -probesize 100M -i - \
        -vcodec libx264 -pix_fmt yuv420p -crf "${H264_CRF}" -preset "${H264_PRESET}" -bf 0 \
        "$qv"
  done

  if [[ -z "${FINAL_LABEL}" ]]; then
    FINAL_LABEL="outv"
  fi

  ffmpeg_cmd=(ffmpeg -y)
  for qv in "${QUAD_TMP_VIDEOS[@]}"; do
    ffmpeg_cmd+=( -i "$qv" )
  done
  if [[ -n "${LOGO_FILE}" ]]; then
    ffmpeg_cmd+=( -i "$LOGO_FILE" )
  fi

  ffmpeg_cmd+=(
    -filter_complex "$COMPLEX_FILTER"
    -map "[${FINAL_LABEL}]"
    -vcodec libx264 -pix_fmt yuv420p -crf "${H264_CRF}" -preset "${H264_PRESET}" -bf 0
    "$OUTPUT"
  )

  "${ffmpeg_cmd[@]}"
else
  if [[ "${USE_COMPLEX}" == true ]]; then
    if [[ -z "${FINAL_LABEL}" ]]; then
      FINAL_LABEL="outv"
    fi

    ffmpeg_cmd=(ffmpeg -y)
    for p in "${PIPES[@]}"; do
      ffmpeg_cmd+=( -r "${GOURCE_FPS}" -f image2pipe -probesize 100M -i "$p" )
    done

    if [[ -n "${LOGO_FILE}" ]]; then
      ffmpeg_cmd+=( -i "$LOGO_FILE" )
    fi

    ffmpeg_cmd+=(
      -filter_complex "$COMPLEX_FILTER"
      -map "[${FINAL_LABEL}]"
      -vcodec libx264 -pix_fmt yuv420p -crf "${H264_CRF}" -preset "${H264_PRESET}" -bf 0
      "$OUTPUT"
    )

    "${ffmpeg_cmd[@]}"
  else
    if [[ -n "${FILTER}" ]]; then
      ffmpeg -y \
        -r "${GOURCE_FPS}" -f image2pipe -probesize 100M -i "${PIPES[0]}" \
        -vf "$FILTER" \
        -vcodec libx264 -pix_fmt yuv420p -crf "${H264_CRF}" -preset "${H264_PRESET}" -bf 0 \
        "$OUTPUT"
    else
      ffmpeg -y \
        -r "${GOURCE_FPS}" -f image2pipe -probesize 100M -i "${PIPES[0]}" \
        -vcodec libx264 -pix_fmt yuv420p -crf "${H264_CRF}" -preset "${H264_PRESET}" -bf 0 \
        "$OUTPUT"
    fi
  fi

  for pid in "${GOURCE_PIDS[@]}"; do
    wait "$pid"
  done
fi

echo "Done: ${OUTPUT}"
